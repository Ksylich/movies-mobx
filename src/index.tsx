import ReactDOM from "react-dom";

// TODO: У тебя этот файлик tsx, поэтому можно обойтись и без render-components-in-dom
import RenderComponents from "./render-components-in-dom";

ReactDOM.render(
  RenderComponents(),
  document.getElementById("root"),
);
